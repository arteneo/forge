import React from "react";

interface WrapperInterface {
    wrapperComponent?: React.ElementType;
    // eslint-disable-next-line
    wrapperComponentProps?: any;
}

export default WrapperInterface;
